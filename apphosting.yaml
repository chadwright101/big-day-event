# use Node 20
# firebase apphosting:secrets:set SMTP_HOST --location europe-west4

runConfig:
  minInstances: 0
env:
  - variable: SMTP_PASS
    secret: SMTP_PASS
  - variable: SMTP_HOST
    secret: SMTP_HOST
  - variable: SMTP_USER
    secret: SMTP_USER
  - variable: SMTP_SEND_TO
    secret: SMTP_SEND_TO
